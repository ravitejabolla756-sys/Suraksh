# Sample Gap Analysis

**SYNTHETIC HACKATHON DEMO DATA**

The API endpoint `GET /registry/summary` calculates camera counts by district, online/offline/degraded ratios, and districts with one or fewer registered cameras. These values describe only the seeded synthetic registry and must not be presented as Gujarat-wide coverage or government inventory.
